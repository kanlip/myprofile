version: '3'
services:
  mongo-server:
    image: 'mongo'
  node-app:
    restart: on-failure
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports: 
      - "8000:3000"
    volumes:
      - /app/node_modules
      - .:/app